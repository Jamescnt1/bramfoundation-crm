/**
 * TEMPORARY BETA CLEANUP MODE.
 *
 * TODO(BETA): Set this to false after the beta dataset has been cleaned, then
 * restore the archive/deactivate actions in the edit screens. Keeping this in
 * one module makes the temporary destructive behavior easy to remove.
 */
export const BETA_PERMANENT_DELETE_ENABLED = true;

